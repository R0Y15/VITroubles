import { ThreadCardProps } from '@/constants'
import React from 'react'

const ThreadCard = ({
    id,
    currentuser,
    parentId,
    content,
    author,
    community,
    createdAt,
    comments }: ThreadCardProps) => {
    return (
        <div>ThreadCard</div>
        // 3:01:50
    )
}

export default ThreadCard